#include "main.h"

/**
 * _abs - give the absolute value of an integer
 * @n: the int to check
 * Return: the absolute value of integer
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	else
		return (-n);
}
